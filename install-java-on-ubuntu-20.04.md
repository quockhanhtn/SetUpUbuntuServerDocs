## Install java <br/><br/>

```bash
sudo apt update
sudo apt install default-jre        # instal Java Runtime Environment (JRE)
sudo apt install default-jdk        # instal Java Development Kit (JDK) 

java -version                       # check JRE version
javac -version                      # check JDK version
```